import "../components/ha-expansion-panel";
import type { HomeAssistant } from "../types";
export declare const regenerateEntityIds: (element: HTMLElement, hass: HomeAssistant, entities: string[]) => Promise<void>;
